---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [".env.example", "src/components/common/BaseButton.vue", "src/components/common/BaseInput.vue", "src/components/common/Modal.vue"]
autonomous: true
requirements: ["SETUP-03"]
must_haves:
  truths:
    - "Environment variables for Telegram bot are defined"
    - "Common UI components are created and available for use"
  artifacts:
    - path: ".env.example"
      provides: "Environment variable templates"
      contains: "VITE_TELEGRAM_BOT_TOKEN"
    - path: "src/components/common/BaseButton.vue"
      provides: "Reusable button component"
  key_links: []
---

<objective>
Configure environment variables for Telegram notifications and setup common UI components.

Purpose: Provides reusable components for UI consistency and configures required credentials template.
Output: Base UI components and `.env.example` file.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup environment variables</name>
  <files>.env.example</files>
  <action>Create `.env.example` (or append to it) with empty variables `VITE_TELEGRAM_BOT_TOKEN=` and `VITE_TELEGRAM_CHAT_ID=`.</action>
  <verify>
    <automated>grep -q VITE_TELEGRAM_BOT_TOKEN .env.example</automated>
  </verify>
  <done>.env.example contains Telegram variables.</done>
</task>

<task type="auto">
  <name>Task 2: Setup BaseButton and BaseInput</name>
  <files>src/components/common/BaseButton.vue, src/components/common/BaseInput.vue</files>
  <action>Create `BaseButton.vue` (supporting variants, loading state) and `BaseInput.vue` (supporting v-model, labels, errors). Use Tailwind classes matching the project's charcoal/cyan theme.</action>
  <verify>
    <automated>ls src/components/common/BaseButton.vue src/components/common/BaseInput.vue</automated>
  </verify>
  <done>BaseButton and BaseInput components are created.</done>
</task>

<task type="auto">
  <name>Task 3: Setup Modal component</name>
  <files>src/components/common/Modal.vue</files>
  <action>Create a reusable `Modal.vue` component with backdrop, close button, title slot, and default slot using standard Vue 3 `<script setup lang="ts">` syntax.</action>
  <verify>
    <automated>ls src/components/common/Modal.vue</automated>
  </verify>
  <done>Modal component is created and ready for import.</done>
</task>

</tasks>

<verification>
Check if `.env.example` has required keys and all common UI components are properly created.
</verification>

<success_criteria>
Env vars are documented and basic UI primitives exist in the common components folder.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
